const sumar = (n1: number, n2: number) => {
    console.log(n1 + n2)
}

sumar(1, 2) /// lo suma el number a string -> infiere en el sentido semantico, no deberia convertir el numero de number a string
export  { sumar }
